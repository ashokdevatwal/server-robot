# Architecture

The agent uses modular collectors for CPU, RAM, Disk, Network, Process, and Service mapping. The scheduler performs threshold checks with sustain/cooldown controls, then triggers root-cause analysis, log collection, and email alerting.
